import { ZalupaDirective } from './limit.directive';

describe('ZalupaDirective', () => {
  it('should create an instance', () => {
    const directive = new ZalupaDirective();
    expect(directive).toBeTruthy();
  });
});
